# Deploy (Caddy) config
DOMAIN=senindomainin.com
ACME_EMAIL=seninmailin@domain.com

# Admin security panel (Caddy)
# Only your IPs should be allowed. Space-separated IPs/CIDRs.
# Example: "1.2.3.4 5.6.7.0/24"
ADMIN_ALLOW_IPS=127.0.0.1 ::1

# Basic Auth user + bcrypt hash of password
ADMIN_BASIC_USER=admin
ADMIN_BASIC_PASS_HASH=$2a$12$CHANGE_ME

# Generate password hash:
# docker run --rm caddy:2 caddy hash-password --plaintext "YOUR_PASSWORD"

# Extra defence: shared secret header (Caddy -> backend) for /admin/* and /api/admin/*
# Set a long random value.
ADMIN_EDGE_TOKEN=CHANGE_ME_RANDOM_LONG
